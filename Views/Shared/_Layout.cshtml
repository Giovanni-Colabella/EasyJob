<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EasyJob</title>
    <link rel="stylesheet" href="~/css/styles.css" asp-append-version="true" />
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    
    <nav class="bg-white shadow-md w-full z-50 relative">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <!-- Logo con animazione -->
            <a href="/" class="text-2xl font-bold text-blue-600 flex items-center">
                <img src="/images/logo.png" alt="Logo EasyJob" class="w-12 h-12 mr-2 animate-bounce">
                <span class="hover:text-blue-700 transition duration-300">EasyJob</span>
            </a>

            <!-- Menu Desktop -->
            <ul class="hidden md:flex space-x-6">
                <li>
                    <a asp-controller="Home" asp-action="Index" class="relative text-gray-700 hover:text-blue-600 transition duration-300 group">
                        Home
                        <span
                            class="absolute bottom-0 left-0 w-0 h-0.5 bg-blue-600 transition-all duration-300 group-hover:w-full"></span>
                    </a>
                </li>
                <li>
                    <a asp-controller="Jobs" asp-action="Index" class="relative text-gray-700 hover:text-blue-600 transition duration-300 group">
                        Lavori
                        <span
                            class="absolute bottom-0 left-0 w-0 h-0.5 bg-blue-600 transition-all duration-300 group-hover:w-full"></span>
                    </a>
                </li>
                <li>
                    <a href="/Contact" class="relative text-gray-700 hover:text-blue-600 transition duration-300 group">
                        Contatti
                        <span
                            class="absolute bottom-0 left-0 w-0 h-0.5 bg-blue-600 transition-all duration-300 group-hover:w-full"></span>
                    </a>
                </li>
            </ul>

            <!-- Icona menu mobile -->
            <button id="mobile-menu-btn" class="md:hidden text-gray-700 focus:outline-none">
                <svg id="menu-icon" class="w-8 h-8 transition-transform duration-300 transform" fill="none"
                    stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16m-7 6h7"></path>
                </svg>
            </button>
        </div>

        <!-- Menu Mobile -->
        <div id="mobile-menu" class="hidden md:hidden bg-white shadow-md absolute top-full left-0 w-full">
            <ul class="text-center py-4 space-y-4">
                <li><a href="/" class="block text-gray-700 hover:text-blue-600 transition duration-300">Home</a></li>
                <li><a href="/Jobs" class="block text-gray-700 hover:text-blue-600 transition duration-300">Lavori</a>
                </li>
                <li><a href="/Contact"
                        class="block text-gray-700 hover:text-blue-600 transition duration-300">Contatti</a></li>
            </ul>
        </div>

    </nav>

    <!-- Script per il menu mobile -->
    <script>
        document.addEventListener("DOMContentLoaded", function () {
        const menuBtn = document.getElementById('mobile-menu-btn');
        const menu = document.getElementById('mobile-menu');
        const icon = document.getElementById('menu-icon');

        menuBtn.addEventListener('click', function () {
        menu.classList.toggle('hidden');
        icon.classList.toggle('rotate-180');
        });
        });
    </script>



    <header class="bg-gradient-to-r from-blue-500 to-purple-600 text-white py-6 px-4 shadow-lg ">
        <div class="max-w-4xl mx-auto text-center">

            <img src="~/images/logo.png" alt="Logo EasyJob" class="mx-auto w-24 h-auto mb-4">

            <h1 class="text-3xl font-extrabold drop-shadow-md">🚀 EasyJob – Il tuo lavoro, più semplice che mai!</h1>
            <h2 class="text-lg mt-2 opacity-90">🔥 Trova, gestisci e organizza le tue opportunità lavorative con
                un'unica piattaforma intuitiva e veloce.</h2>
        </div>
    </header>

    <main class="bg-gradient-to-r from-blue-500 to-purple-600 text-white py-6 px-4 shadow-lg min-h-[100vh] ">
        @RenderBody()
    </main>

    <f<footer class="bg-gray-900 text-gray-300 py-6 mt-10">
        <div class="max-w-5xl mx-auto text-center space-y-4">
            <h3 class="text-lg font-semibold text-gray-900">EasyJob</h3>
            <p class="text-sm text-gray-900">Copyright 2025 -Giovanni Colabella</p>
        </div>
        </footer>

</body>
</html>